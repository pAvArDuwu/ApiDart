name: miprimeraapi
description: A server app using the shelf package and Docker.
version: 1.0.0

environment:
  sdk: ^3.11.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  mysql_client: ^0.0.27

dev_dependencies:
  http: ^1.2.2
  lints: ^6.0.0
  test: ^1.25.6
